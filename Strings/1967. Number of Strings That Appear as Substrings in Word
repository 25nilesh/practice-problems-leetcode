class Solution {
public:
    int numOfStrings(vector<string>& patterns, string word) {
        int result = 0;
        for(string s: patterns){
            if(word.find(s)!=-1){
                result++;
            }
        }
        return result;
    }
};
