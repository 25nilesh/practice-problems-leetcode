class Solution {
public:
    bool canJump(vector<int>& nums) {
        int n=nums.size();
        int maxEnd=0;
        for(int i=0;i<n;i++){
            if(i>maxEnd) return false;
            maxEnd=max(maxEnd,i+nums[i]);
        }
        return true;
    }
};
